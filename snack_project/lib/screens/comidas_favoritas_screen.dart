import 'package:flutter/material.dart';

class ComidaFavorita extends StatelessWidget {
  const ComidaFavorita({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Minhas Comidas Favoritas'),
    );
  }
}
